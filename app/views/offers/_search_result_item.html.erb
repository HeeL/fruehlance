<div class="search-result-item<%=' odd' if offer_counter.odd?%>">
  <h2><%=link_to offer.title, offer.offer_url, target: '_blank'%></h2>
  <i><%=nice_date(offer.posted_at)%></i>
  <%=simple_format(offer.desc)%>
</div>