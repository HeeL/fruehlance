<%@hourly_stats.each do |source_id, data|%>
  hourly_chart.addSeries({name: '<%=Offer.source_display_name(Offer.source_name(source_id.to_i))%>', data: <%=data%>}, false);
<%end%>
hourly_chart.redraw();
